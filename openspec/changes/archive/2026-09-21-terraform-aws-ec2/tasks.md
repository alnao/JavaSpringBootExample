## 1. Scheletro e provider

- [x] 1.1 Creare `script/aws-terraform-ec2/versions.tf` (required_version >= 1.10, provider aws ~> 5.0, tls ~> 4.0, local ~> 2.5, http ~> 3.4, `backend "s3" {}` parziale), `variables.tf` (region, environment con validation dev|test|production, instance_type, db_instance_class, db_username, db_password sensitive senza default, db_name, ssh_allowed_cidr, key_name), `locals.tf` (nomi identici allo stack bash, common_tags, marker_key) e `data.tf` (VPC default, subnet, AMI Amazon Linux 2, http checkip). Verifica: `terraform fmt -check` sulla cartella.
- [x] 1.2 Aggiornare `.gitignore` con `**/.terraform/`, `*.tfstate`, `*.tfstate.*`, `*_override.tf`, `backend.hcl`, `*.tfvars` e l'eccezione `!*.tfvars.example`; creare `terraform.tfvars.example` con i valori non sensibili commentati. Verifica: `git check-ignore` risponde per `script/aws-terraform-ec2/terraform.tfstate` e `backend_override.tf`, non per `terraform.tfvars.example`.

## 2. Risorse dello stack (design D2, D4-D7)

- [x] 2.1 `iam.tf`: ruolo `gestioneannotazioni-ec2-role` (assume role ec2), 4 attachment (RDSFullAccess, DynamoDBFullAccess, EC2ContainerRegistryReadOnly, SQSFullAccess), instance profile `gestioneannotazioni-ec2-profile`, `Name` su ruolo e profile. Verifica: `terraform validate` (dopo 1.1 e `init -backend=false`).
- [x] 2.2 `network.tf`: security group `gestioneannotazioni-sg` nella VPC di default, regole ingress `for_each` (3306, 6379, 8080, 8086, 8087 da 0.0.0.0/0), SSH dal CIDR calcolato (`coalesce(var.ssh_allowed_cidr, ip/32)`), egress totale, `Name`. Verifica: `terraform validate`; rilettura del confronto porte con `script/aws-ec2/start-all.sh` righe 73-81 e 284.
- [x] 2.3 `database.tf`: `aws_rds_cluster` `gestioneannotazioni-cluster` (aurora-mysql, master da variabili, SG, skip_final_snapshot, apply_immediately) e `aws_rds_cluster_instance` `gestioneannotazioni-instance` (`var.db_instance_class`, publicly_accessible), `Name` su entrambi. Verifica: `terraform validate`.
- [x] 2.4 `dynamodb.tf`: tre tabelle con nomi, chiavi, billing e GSI `idAnnotazione-index` identici allo script (PAY_PER_REQUEST per `annotazioni` e `annotazioni_storico`, PROVISIONED 5/5 con GSI 5/5 projection ALL per `annotazioni_storicoStati`). Verifica: `terraform validate`; confronto con `script/aws-ec2/start-all.sh` righe 153-203.
- [x] 2.5 `sqs.tf` (due code standard con `Name`) e `cache.tf` (subnet group sulle subnet della VPC di default, cluster Redis cache.t3.micro 1 nodo porta 6379 con SG, `Name`). Verifica: `terraform validate`.
- [x] 2.6 `compute.tf`: `tls_private_key` RSA 4096, `aws_key_pair` `gestioneannotazioni-terraform-key`, `local_sensitive_file` `.pem` 0400, `aws_instance` (AMI dal data source, `var.instance_type`, instance profile, SG, key, `user_data = templatefile(...)`, `user_data_replace_on_change`, `depends_on` sull'istanza Aurora, tags con `Name=gestioneannotazioni-terraform-ec2` e marcatore `gestioneannotazioni-terraform-app=true`, `root_block_device.tags` con merge dei common_tags e `Name=…-volume`). Verifica: `terraform validate`; `grep -c "gestioneannotazioni-app" compute.tf` = 0.
- [x] 2.7 `user_data.sh.tftpl` trasposto dal heredoc di `script/aws-ec2/start-all.sh` righe 322-390 (Docker, mysql client, attesa Aurora 3×30 s, esecuzione di `${init_sql}`, `docker run` con le stesse variabili d'ambiente, retry 3×). Verifica: render locale con `terraform console` o `templatefile` in un `.tf` temporaneo con valori finti, `bash -n` sul risultato, `diff` delle variabili d'ambiente del `docker run` con lo script bash.
- [x] 2.8 `outputs.tf`: `ec2_public_ip`, `app_url`, `ssh_command`, `aurora_endpoint`, `redis_endpoint`, `sqs_export_queue_url`, `sqs_import_queue_url`, `private_key_file`. Verifica: `terraform validate`; nessun output marcato non-sensitive espone la password.

## 3. Wrapper e test

- [x] 3.1 `tf-init.sh` con `terraform_init_backend` (design D3: `TF_STATE_BUCKET` default `alnao-dev-terraform` e vuoto = locale via `backend_override.tf`, `TF_STATE_REGION`, `TF_STATE_KEY`, `use_lockfile=true`, `-reconfigure`). Verifica: dry-run con stub `terraform` nei tre casi (default → 4 `-backend-config`; `TF_STATE_BUCKET=altro` → bucket sostituito; vuoto → `backend_override.tf` scritto e nessun `-backend-config`); `bash -n`.
- [x] 3.2 `start-all.sh` (export `TF_VAR_environment`, `TF_VAR_db_password` da `DB_PASS`, `TF_VAR_region`; init; `apply -auto-approve -input=false`; `output`) e `stop-all.sh` (init; `destroy -auto-approve -input=false`; `rm -f *.pem`), eseguibili dalla root come gli altri. Verifica: dry-run con stub `terraform`; `bash -n`; `ENVIRONMENT=collaudo` arriva a Terraform come `TF_VAR_environment=collaudo` (la validazione è nel `.tf`).
- [x] 3.3 `test-aws-terraform-ec2.sh` derivato da `script/aws-ec2/test-aws-ec2.sh`: filtro `tag:gestioneannotazioni-terraform-app`, esiti registrati con `lib-report.sh` (`report_run_inizio "aws (terraform-ec2)"`, `test_ok`/`test_ko`, `report_chiusura`), errore esplicito se non trova l'istanza, richiamo di `test-prenotazione-annotazione.sh`. Verifica: `bash -n`; dry-run con stub `aws` che risponde `None` → messaggio "istanza non trovata" ed exit ≠ 0.

## 4. Documentazione

- [x] 4.1 `script/aws-terraform-ec2/README.md` (uso rapido, variabili, backend e casi `TF_STATE_*`, key pair e alternativa esistente, differenze dallo stack bash, errori da evitare) e sezione "🧱 Esecuzione su AWS EC2 con Terraform" in `PlatformAws.md` con rimando alla sezione tag. Verifica: rilettura; ancore interne funzionanti.
- [x] 4.2 `Roadmap.md`: spuntare "📜 Script terraform" specificando "stack EC2 (change `terraform-aws-ec2`), ECS/EKS 🚧"; `openspec/project.md`: aggiungere `aws-terraform-ec2/` all'elenco delle cartelle di deploy. Verifica: rilettura.

## 5. Verifica finale

- [x] 5.1 In `script/aws-terraform-ec2`: `terraform fmt -check -recursive`, `terraform init -backend=false` (versiona `.terraform.lock.hcl`), `terraform validate`; `bash -n` su tutti gli `.sh`; `git status` mostra solo i file previsti (niente `.terraform/`, state, override, `.pem`). Verifica: tutti i comandi con exit 0.
- [x] 5.2 Regressione: `./script/automatic-test/test-aws-onprem.sh` (locale, nessun costo) a conferma che il runtime del profilo `aws` è invariato; poi, **a cura dell'utente perché genera costi**: `./script/aws-ec2/stop-all.sh` se lo stack bash è attivo → `./script/aws-terraform-ec2/start-all.sh` → `./script/aws-terraform-ec2/test-aws-terraform-ec2.sh` → `aws resourcegroupstaggingapi get-resources --tag-filters Key=Project,Values=Annotazioni.aws-terraform-ec2` (tutte le risorse, `ManagedBy=Terraform`) → `./script/aws-terraform-ec2/stop-all.sh` → `get-resources` vuoto. Verifica: regressione locale verde; sullo stack reale gli scenari "Apply su account vuoto", "Test dopo apply" e "Destroy dopo apply" della spec soddisfatti.
