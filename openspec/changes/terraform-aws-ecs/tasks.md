## 1. Helper condiviso e scheletro

- [x] 1.1 Spostare `script/aws-terraform-ec2/tf-init.sh` in `script/aws-tf-init.sh` con il nome dello stack come argomento del `source` (chiave di default `annotazioni/<stack>/terraform.tfstate`); aggiornare `script/aws-terraform-ec2/{start-all,stop-all}.sh` (`source "$(dirname "$0")/../aws-tf-init.sh" aws-terraform-ec2`) e il README di quella cartella. Verifica: dry-run dei due wrapper EC2 con stub `terraform` → stessa riga di `init` di prima (bucket, chiave `annotazioni/aws-terraform-ec2/...`, region, `use_lockfile`); `bash -n`; file vecchio assente.
- [x] 1.2 Creare `script/aws-terraform-ecs/versions.tf` (backend s3 parziale, `default_tags`), `variables.tf` (region, environment con validation, db_engine_version default null, db_instance_class, db_username, db_password sensitive, db_name, image_tag, task_cpu, task_memory, desired_count), `locals.tf` (nomi identici a `script/aws-ecs/start-all.sh` righe 25-47, common_tags con `Project=Annotazioni.aws-terraform-ecs`, mappe porte), `data.tf` (VPC default, subnet, caller identity, region) e `terraform.tfvars.example`. Verifica: `terraform fmt -check`.

## 2. Risorse dello stack (design D2, D4-D7)

- [x] 2.1 `ecr.tf` (repository `gestioneannotazioni`, `force_delete = true`, `Name`) e `iam.tf` (ruolo task con DynamoDBFullAccess/RDSFullAccess/SQSFullAccess, ruolo execution con AmazonECSTaskExecutionRolePolicy/CloudWatchLogsFullAccess/SQSFullAccess, assume role `ecs-tasks.amazonaws.com`, `Name`). Verifica: `terraform init -backend=false` + `validate`.
- [x] 2.2 `network.tf`: SG `gestioneannotazioni-sg` con regole 8080/443/22 da `0.0.0.0/0` e 3306/6379 con `referenced_security_group_id` = SG stesso, egress totale, `Name`. Verifica: `validate`; confronto con `script/aws-ecs/start-all.sh` righe 137-141 e 348.
- [x] 2.3 `database.tf` (cluster `gestioneannotazioni-aurora-cluster` con `database_name`, `engine_version = var.db_engine_version`, istanza `gestioneannotazioni-aurora-instance` privata), `dynamodb.tf`, `sqs.tf`, `cache.tf` (identici allo stack EC2 Terraform, con `Name`). Verifica: `validate`; `diff` di dynamodb/sqs/cache con la cartella EC2 mostra solo eventuali commenti.
- [x] 2.4 `ecs.tf`: `aws_ecs_cluster` `gestioneannotazioni-cluster`, `aws_cloudwatch_log_group` `/ecs/gestioneannotazioni-app`, `aws_ecs_task_definition.app` (`gestioneannotazioni-task`, Fargate, cpu/memory da variabili, ruoli, `container_definitions` con le 16 variabili nello stesso ordine del heredoc bash e `awslogs`), `aws_ecs_service` `gestioneannotazioni-service` (desired da variabile, `awsvpc` su tutte le subnet della VPC di default, IP pubblico, `propagate_tags = "SERVICE"`, `Name`). Verifica: `validate`; render di `container_definitions` in una config temporanea con valori finti → JSON valido e `diff` dei 16 nomi di variabile con `script/aws-ecs/start-all.sh` righe 418-435 = nessuna differenza.
- [x] 2.5 `init-db.tf`: `aws_ecs_task_definition.init` (`gestioneannotazioni-mysql-init`, `mysql:8.0`, 256/512, execution role, log prefix `init`, `environment` `DB_HOST`/`DB_USER`/`MYSQL_PWD`, `command` con connessione → `base64 -d` dell'SQL locale → `mysql --force` → verifica `users`, `depends_on` istanza Aurora e cluster ECS). Verifica: `validate`; render del `command` con valori finti, decodifica del base64 e `diff` con `script/init-database/init-mysql.sql` = identico; `bash -n` dello script shell del comando.
- [x] 2.6 `outputs.tf`: `ecr_repository_url`, `ecs_cluster_name`, `ecs_service_name`, `app_task_definition_arn`, `init_task_definition` (family:revision), `init_task_tags_json`, `security_group_id`, `subnet_ids` (stringa separata da virgole), `aurora_endpoint`, `redis_endpoint`, `sqs_export_queue_url`, `sqs_import_queue_url`, `log_group_name`. Verifica: `validate`; nessun output espone la password.

## 3. Wrapper e test (design D2, D3, D9)

- [x] 3.1 `start-all.sh`: controllo prerequisiti (`docker`, `jq`, `terraform`) prima di tutto; `TF_VAR_*` da `ENVIRONMENT`, `DB_PASS`, `AWS_REGION`, `IMAGE_TAG`; `source ../aws-tf-init.sh aws-terraform-ecs`; apply mirato ECR + task init; login ECR, `docker build` dalla root del progetto, tag e push; `run-task` dell'init con attesa STOPPED e controllo `exitCode`; apply completo; attesa task RUNNING (20 × 30 s) e IP pubblico via ENI; stampa URL, output e comandi utili. Verifica: `bash -n`; dry-run con stub `terraform`/`aws`/`docker` che logga la sequenza (init → apply -target → ecr login → build → tag → push → run-task → describe-tasks → apply → list-tasks → describe-network-interfaces); dry-run con `docker` assente dal PATH → uscita prima di `terraform init`; dry-run con exitCode 1 dell'init → uscita con errore prima dell'apply completo.
- [x] 3.2 `stop-all.sh` (stesso init, `destroy -auto-approve -input=false`, messaggio finale). Verifica: `bash -n`; dry-run con stub → `init` + `destroy`.
- [x] 3.3 `test-aws-terraform-ecs.sh` derivato da `script/aws-ecs/test-aws-ecs.sh`: 10 fasi con `test_ok`/`test_ko` (`report_run_inizio "aws (terraform-ecs)"`, `report_chiusura` nel trap), errore se nessun task RUNNING, richiamo di `test-prenotazione-annotazione.sh`. Verifica: `bash -n`; dry-run con stub `aws` che risponde `None` a `list-tasks` → `test_ko` "task non trovato" ed exit 1 con riepilogo.

## 4. Documentazione

- [x] 4.1 `script/aws-terraform-ecs/README.md` (uso rapido, sequenza del wrapper e perché l'apply è in due passi, variabili incluse `IMAGE_TAG` e `TF_VAR_db_engine_version`, state, differenze dal bash, avviso Aurora 5.7, errori da evitare) e sezione "🧱 Esecuzione su AWS ECS Fargate con Terraform" in `PlatformAws.md` con rimandi alla sezione tag e al README. Verifica: rilettura; ancore funzionanti.
- [x] 4.2 `Roadmap.md`: voce "Script terraform per gli stack ECS ed EKS" → "✅ Script terraform per lo stack ECS Fargate (`script/aws-terraform-ecs`, change `terraform-aws-ecs`)" e "🚧 Script terraform per lo stack EKS"; `openspec/project.md`: aggiungere `aws-terraform-ecs/` all'elenco. Verifica: rilettura.

## 5. Verifica finale

- [x] 5.1 In `script/aws-terraform-ecs`: `terraform fmt -check -recursive`, `terraform init -backend=false` (versiona `.terraform.lock.hcl`), `terraform validate`; `bash -n` su tutti gli `.sh` di `aws-terraform-ecs`, `aws-terraform-ec2` e `script/aws-tf-init.sh`; `git status` mostra solo i file previsti. Verifica: tutti i comandi con exit 0.
- [x] 5.2 Regressione: `./script/automatic-test/test-aws-onprem.sh` (locale, nessun costo); poi, **a cura dell'utente perché genera costi**: stack bash ECS/EC2 e Terraform EC2 spenti → `./script/aws-terraform-ecs/start-all.sh` → `./script/aws-terraform-ecs/test-aws-terraform-ecs.sh` → `aws resourcegroupstaggingapi get-resources --tag-filters Key=Project,Values=Annotazioni.aws-terraform-ecs` → `./script/aws-terraform-ecs/stop-all.sh` → `get-resources` vuoto. Verifica: regressione locale verde; sullo stack reale gli scenari "Apply su account vuoto", "Database pronto dopo start-all", "Test dopo apply" e "Destroy dopo apply" della spec soddisfatti.
