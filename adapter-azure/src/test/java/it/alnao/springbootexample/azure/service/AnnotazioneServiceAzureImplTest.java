package it.alnao.springbootexample.azure.service;

import it.alnao.springbootexample.core.domain.AnnotazioneCompleta;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

import static org.junit.jupiter.api.Assertions.*;

//@SpringBootTest(classes = it.alnao.springbootexample.app.GestioneAnnotazioniApplication.class)
//@ActiveProfiles("azure")
class AnnotazioneServiceAzureImplTest {
    /*
    @Autowired
    AnnotazioneServiceAzureImpl service;

    @Test
    void testCreaAnnotazione() {
        AnnotazioneCompleta result = service.creaAnnotazione("test nota", "descrizione di test", "utenteTest");
        assertNotNull(result);
        assertNotNull(result.getAnnotazione());
        assertNotNull(result.getMetadata());
        assertEquals("test nota", result.getAnnotazione().getValoreNota());
        assertEquals("utenteTest", result.getMetadata().getUtenteCreazione());
    }
         */
}
